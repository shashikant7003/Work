kind = "web"
previewPath = "/"
title = "RAK — Video Editor Portfolio"
version = "1.0.0"
id = "artifacts/rak"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18483

[services.development]
run = "pnpm --filter @workspace/rak run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/rak", "run", "build" ]
publicDir = "artifacts/rak/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18483"
BASE_PATH = "/"
